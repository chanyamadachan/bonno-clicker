# backend/ 配下はデフォルトで一切Web公開しない(config.local.php等の秘密情報・DDL・cronスクリプトを含むため)。
# backend/public/ のみ backend/public/.htaccess で明示的に許可を上書きする。
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
